# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
set $mod Mod4
font pango:SF Pro Display 10

exec --no-startup-id picom
exec --no-startup-id hsetroot
exec --no-startup-id flameshot
exec --no-startup-id skypeforlinux
exec --no-startup-id copyq
exec --no-startup-id firefox
exec --no-startup-id mattermost
exec --no-startup-id kitty --name scratch-term
exec --no-startup-id yubioath-desktop
exec --no-startup-id telegram-desktop -startintray
exec --no-startup-id slack -u
exec --no-startup-id xss-lock "/home/sam/.config/i3/lock-screen"
exec --no-startup-id pasystray
exec --no-startup-id blueman-applet
exec --no-startup-id signal-desktop --start-in-tray

exec_always --no-startup-id xmodmap -e "clear lock"
exec_always --no-startup-id xmodmap -e "keycode 9 = Caps_Lock NoSymbol Caps_Lock"
exec_always --no-startup-id xmodmap -e "keycode 66 = Escape NoSymbol Escape"
exec_always --no-startup-id xmodmap -e "keysym Menu = Super_R"

for_window [class="Slack"] floating enable border normal
for_window [class="Skype"] floating enable border normal
for_window [class="Git-gui" instance="git-gui"] floating enable
for_window [title="Terminator Preferences"] floating enable
for_window [class="mattermost-nativefier"] floating enable, move to workspace 3
for_window [class="simplescreenrecorder"] floating enable
for_window [instance="scratch-term"] floating enable, move to scratchpad
for_window [class="Yubico Authenticator" window_type="normal"] floating enable, move to scratchpad
for_window [class="mattermost-nativefier"] floating enable, move to scratchpad
for_window [class="Telegram"] floating enable

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Shift+Return exec i3-sensible-terminal
bindsym $mod+Return exec /home/sam/.config/i3/i3-plus layout_exec i3-sensible-terminal

# kill focused window (usually shift q)
bindsym $mod+q kill

bindsym $mod+d exec rofi -combi-modi window#run -show combi -modi combi

bindsym $mod+h split h
bindsym $mod+v split v
bindsym $mod+f fullscreen

# center a floating window
bindsym $mod+c exec "/home/sam/.config/i3/i3-plus smart_center 1830x2100,2030x2100,2230x2100,2500x2100"

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# change focus
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon exec /home/sam/.config/i3/i3-plus focus right
bindsym $mod+j exec /home/sam/.config/i3/i3-plus focus left

# alternatively, you can use the cursor keys:
bindsym $mod+Left exec /home/sam/.config/i3/i3-plus focus left
bindsym $mod+Right exec /home/sam/.config/i3/i3-plus focus right
bindsym $mod+Down focus down
bindsym $mod+Up focus up

bindsym $mod+Shift+Left [con_id="__focused__" tiling] move left;  [con_id="__focused__" floating] move to output left, focus
bindsym $mod+Shift+Right [con_id="__focused__" tiling] move right; [con_id="__focused__" floating] move to output right, focus
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

bindsym $mod+comma exec pkill -USR2 -f 'ruby bin/unicorn'

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

bindsym $mod+Ctrl+Right "move workspace to output right"
bindsym $mod+Ctrl+Left "move workspace to output left"

# resize commands
bindsym $mod+Mod1+Right exec --no-startup-id /home/sam/.config/i3/i3-resize right
bindsym $mod+Mod1+Left exec --no-startup-id /home/sam/.config/i3/i3-resize left
bindsym $mod+Mod1+Up exec --no-startup-id /home/sam/.config/i3/i3-resize up
bindsym $mod+Mod1+Down exec --no-startup-id /home/sam/.config/i3/i3-resize down

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    tray_output DP-0
    font pango:DejaVu Sans Mono, Font Awesome 5 Free
    position bottom
    status_command /bin/i3status-rs /home/sam/.config/i3/status.toml
    colors {
        separator #666666
        background #222222
        statusline #dddddd
        focused_workspace #0088CC #0088CC #ffffff
        active_workspace #333333 #333333 #ffffff
        inactive_workspace #333333 #333333 #888888
        urgent_workspace #2f343a #900000 #ffffff
   }
}

bindsym $mod+Shift+d exec cd /home/sam/Source/discourse && i3-sensible-terminal -e nvim -c ':NERDTree|:wincmd w|:vsplit'

# screenshot apps
bindsym Print exec "flameshot gui"
bindsym $mod+Print exec "/bin/bash -c '/home/sam/.config/i3/countdown 3 && sleep 0.2 && flameshot gui'"
# full desktop screenshot
bindsym $mod+Shift+Print exec "/home/sam/.config/i3/i3-plus screenshot"

workspace 1 output DP-2
workspace 2 output DP-0
workspace 3 output HDMI-0

# scratchpad apps
bindsym $mod+p [instance="scratch-term"] scratchpad show
bindsym $mod+b [class="Firefox"] scratchpad show
bindsym $mod+y [class="Yubico Authenticator"] scratchpad show
bindsym $mod+m [class="mattermost-nativefier"] scratchpad show

bindsym $mod+x mode "exit: [l]ogout, [r]eboot, [s]hutdown"
mode "exit: [l]ogout, [r]eboot, [s]hutdown" {
  bindsym l exec i3-msg exit
  bindsym r exec systemctl reboot
  bindsym s exec systemctl shutdown
  bindsym Escape mode "default"
  bindsym Return mode "default"
}

focus_follows_mouse no
